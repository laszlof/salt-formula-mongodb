{% set server = salt['grains.filter_by']({
    'RedHat': {
        'pkgs': ['mongodb-org', 'mongodb-org-tools'],
        'service': 'mongod',
        'lock_dir': "/var/lock/mongodb",
        'logging': {},
        'bind': {
          'address': '127.0.0.1',
          'port': 27017
        },
        'authorization': {},
        'admin': {
          'username': 'root'
        }
    },
}, merge=salt['pillar.get']('mongodb:server')) %}
